/* CSS Custom Properties (CSS Variables) */
:root {
  --color-primary: #FFD700;
  --color-secondary: #FF0000;
  --color-text: #FFFFFF;
  --color-background: #000000;
  --color-background-gradient: linear-gradient(to bottom, rgb(0, 0, 0), rgb(80, 80, 80));
  --color-background-mobile: linear-gradient(to bottom, rgb(0, 0, 0), rgb(50, 50, 50));
  --font-primary: "Raleway", sans-serif;
  --font-weight-normal: 400;
  --font-weight-bold: 800;
  --border-radius: 10px;
  --transition-speed: 0.3s;
}

/* Utility classes using CSS variables */
.text-gold {
  color: var(--color-primary);
}
.text-white {
  color: var(--color-text);
}
.text-red {
  color: var(--color-secondary);
}
.font-weight-bold {
  font-weight: bold; 
}
.cp {
  cursor: pointer;
}

/* Google Fonts import */
@import url('https://fonts.googleapis.com/css2?family=Raleway:ital,wght@0,100..900;1,100..900&display=swap');

/* Typography */
.header-font {
  font-family: var(--font-primary);
  font-optical-sizing: auto;
  font-weight: var(--font-weight-bold);
  font-style: normal;
}

/* Global font settings */
* {
  font-family: var(--font-primary);
  font-optical-sizing: auto;
  font-weight: var(--font-weight-normal);
  font-style: normal;
}

/* Reset basisstijlen */
* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
  -webkit-tap-highlight-color: transparent; /* Voorkomt highlight bij tikken */
  touch-action: manipulation; /* Zorgt ervoor dat alleen scroll- en zoomacties mogelijk zijn */
  user-select: none; /* Voorkomt tekstselectie */
  -webkit-user-drag: none; /* Voorkomt dat elementen kunnen worden gesleept */
}

p {
  margin-bottom: 15px;
  color: white;
  font-size: large;
  word-wrap: break-word; 
  word-break: break-word;
}

/* Container breedte beperken om overschrijding te voorkomen */
.container {
  max-width: 100%;
  overflow-x: hidden;
  padding: 15px;
}

/* Specifieke fix voor mobiele apparaten */
@media (max-width: 768px) {
  .container {
    padding: 0 15px;
  }
}
